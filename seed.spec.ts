import { test } from './fixtures/auth';

test.describe('Seed', () => {
  test('authenticated session', async ({ loggedInPage }) => {
    // Entry point for test-generator agent.
    // Replace this body with the app state required by your scenario.
  });
});
