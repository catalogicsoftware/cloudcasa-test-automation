node_modules/
playwright-report/
test-results/
allure-report/
allure-results/
.env
.vscode
.claude
CLAUDE.md
specs
allure-history.jsonl
allure*
docs

# Local-only Jenkins-in-Docker stack (used to rehearse the pipeline locally)
ci/local/

# Daily Mailinator call counter, written by utils/mailinator-quota.ts
.mailinator-quota/

# Cross-worker bucket locks, written by utils/bucket-lock.ts
.storage-locks/
